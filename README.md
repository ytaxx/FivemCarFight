# FivemCarFight
Vehicle-Fight for FiveM: protects against vehicle-based combat exploits by restricting firing based on vehicle whitelist, aiming angle, and speed; adds collision particles and camera shake, disables action-mode, and runs a client heartbeat to detect event spam/resource-stopper attempts. Configurable in config.lua. Supports easy tuning and defaults.
